== `commandunit`: Installation Handbook

Place the following script in one of directories on your `PATH` environment variable and give `755` permission (`chmod 755`).

[source, bash]
.commandunit
----
include::resources/examples/commandunit[commandunit]
----

Or download it from link:resources/examples/commandunit[here].

[.text-right]
// suppress inspection "AsciiDocLinkResolve"
link:index.html[top]