---
"$extends":
  - base/normal.json
when:
  description:
    - Run a failing test and make sure it is reported correctly.
  cmd: "${OUTDIR}/main/scripts/bin/commandunit"
  args:
    - QUOTE2:--test-srcdir=${OUTDIR#/var/lib/commandunit}/test/scripts/lv2/failing
then:
  description":
    - The message is printed to stdout.
  stdout:
    present:
      - REGEX:1\.\.1
      - REGEX:not ok 1 test-failing\.json
  stderr:
    present:
      - REGEX:TESTS FAILED
      - REGEX:ERROR
