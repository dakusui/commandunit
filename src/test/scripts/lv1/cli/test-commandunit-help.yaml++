---
"$extends":
  - base/normal.json
when:
  description:
    - Just print a help message of commandunit
  cmd: "${COMMANDUNIT_INDOCKER:+${COMMANDUNIT_HOSTFSROOT_MOUNTPOINT}}${PROJECT_DIR}/out/main/scripts/bin/commandunit"
  args:
    - --help
then:
  description":
    - The message is printed to stdout.
  stdout:
    present:
      - REGEX:Usage.+commandunit.+
  stderr:
    absent:
      - REGEX:.+
